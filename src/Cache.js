import {writable} from 'svelte/store'

export const UserCache = writable({});
export const AssetCache = writable({});
export const CollectionCache = writable({});
export const TokenCache = writable({});